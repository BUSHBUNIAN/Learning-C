#include<stdio.h>
#include<math.h>

int main()
{
    int a=5, b=7; //    Value of 'a' is 5 ; Value of 'b' is 7

    a=b; // Value of 'b' (which is 7) is assigned to 'a' ; Now a=7

    printf("%i",a);
    
    return 0;
}