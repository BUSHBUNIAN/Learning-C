#include<stdio.h>
#include<math.h>    //You can write this 'math.h' header file. Without this line this code can still work out

int main()
{  /* Here we can alternatively write as: 

    1.Declare and Initialize the variable 
    
    int main()
    { 
        a=4,b=4;
        printf("%i",a==b);
        return 0;
    
    }      */
    
    printf("%i", 4==4);     // Output is 1; It shows that it's TRUE; meaning '4' is equal to '4'
    return 0;
}