#include<stdio.h>
#include<math.h>

int main()
{
    int X; int Y=X; /*  Variable 'X' is declared and then we've used TERMINATOR(;) and then variable 'Y' is declared and now variable 'X'is used
    meaning value of variable 'X' is assigned to variable 'Y' ; 
    
    It obeys the TYPE DECLARATION RULE, hence " int X; int Y=X; "  is a valid statement in C Programming Language   */
    
}
