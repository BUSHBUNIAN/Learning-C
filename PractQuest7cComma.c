#include<stdio.h>
#include<math.h>

int main()
{
    int X, Y=X; /*    Variable 'X' is declared and then we've used COMMA(,) and then Varaiable 'Y' is declared by the same integer type (i.e int)
     and now Variable 'X' is used meaning value of Variable 'X' is assigned to Variable 'Y' ;
    
    It obeys the TYPE DECLARATION RULE, hence " int X, Y=X; "  is a valid statement in C Programming Language     */
    
}